<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apr7.sponge.dao.HistoryDataDao">
	<insert id="addHistoryDatas">
		INSERT INTO test.T_HISTORY_DATA(FWORKSHOP_ID,FDATETIME,FCOMPANY_ID,FRTD_DATA,FSTATUS_DATA,FDATA_PROTOCOL)
		VALUES
		<foreach collection="historyDatas" item="item" separator=",">
		(#{item.workshopId},#{item.dateTime},#{item.companyId},'','',1)
		</foreach>
	</insert>

	<select id="listHistoryDataByCompanyId" resultType="com.apr7.sponge.model.HistoryData">
		SELECT
			  data.FWORKSHOP_ID workshopId
			, data.FDATETIME dateTime
			, data.FRTD_DATA rtdData
			, data.FSTATUS_DATA statusData
			, data.FDATA_PROTOCOL dataProtocol
		FROM
			T_HISTORY_DATA data
		WHERE
			data.FCOMPANY_ID = #{companyId}
		<if test="workshopId != null">
			AND data.FWORKSHOP_ID = #{workshopId}
		</if>
		<![CDATA[
			AND #{startTime} <= data.FDATETIME AND data.FDATETIME < #{endTime}
		]]>
		ORDER BY
			data.FDATETIME DESC;
	</select>
</mapper>